package discount;

public class DiscountPercentOff {

	private double total=0;
	public DiscountPercentOff( double limit, double percent){
		if(originalTotal>limit){
			total=originalTotal*(1-percent/100);
		}else{
			total=originalTotal;
		}
	}
	public double getTotal(){
		return total;
	}
}
